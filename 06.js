// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.2); //ceil siempre redondea hacia arriba.
resultado = Math.floor(2.2); //floor siempre redondea hacia abajo.
resultado = Math.sqrt(144);
resultado = Math.abs(-144);//convierte numero en positivo
resultado = Math.min(3, 5, 8, 1, 2, 10); //selecciona el numero mas bajo del listado.
resultado = Math.max(3, 5, 8, 1, 2, 10); //selecciona el numero mas alto del listado.
resultado = Math.floor( Math.random() * 30);
console.log(resultado);

